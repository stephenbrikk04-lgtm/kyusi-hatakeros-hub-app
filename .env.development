# Local dev only: point the app at the local backend.
# Production builds (vite build) ignore this file, so the GitHub Pages build stays local-only
# until a real backend URL is provided via .env.production / build env.
VITE_API_URL=http://localhost:8787
