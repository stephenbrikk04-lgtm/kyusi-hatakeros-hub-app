# Cloudflare Pages config. The app builds to dist/ and Pages Functions live in functions/.
# The D1 database binding ("DB" → khth-db) is configured in the Pages dashboard
# (Settings → Bindings), so it is NOT declared here (a database_id would be required if it were).
name = "kyusi-hatakeros-hub"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
